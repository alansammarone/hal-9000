System hostname = {{VM_HOSTNAME}}
Password for root = {{ROOT_PASSWORD_HASH}}
Change the default console to com0 = yes
Which speed should com0 use = 115200
Setup a user = {{VM_USER}}
Password for user {{VM_USER}} = {{USER_PASSWORD_HASH}}
Public ssh key for user {{VM_USER}} = {{SSH_PUBLIC_KEY}}
What timezone are you in = {{TIMEZONE}}
Network interfaces = vio0
IPv4 address for vio0 = dhcp
IPv6 address for vio0 = none
Which disk is the root disk = sd0
Use (W)hole disk or (E)dit the MBR = whole
Use (A)uto layout, (E)dit auto layout, or create (C)ustom layout = auto
Set name(s) = -game* -x*
Location of sets = cd0
