# uv
export PATH="/Users/alan/.local/bin:$PATH"

# Editor configuration
export EDITOR='nvim'
export VISUAL='nvim'

# Semi-sensitive environment variables (API tokens, etc.)
[ -f ~/.zsh_secrets ] && source ~/.zsh_secrets
